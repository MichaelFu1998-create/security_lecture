# --- Runtime (the unified vulnerable app + attacker server) ---------------
Flask>=3.0
Werkzeug>=3.0

# --- Optional: load SECURE / APP_SECRET_KEY from a .env file --------------
# python-dotenv>=1.0

# --- Dev tooling used by static_analysis/run_scans.sh --------------------
bandit>=1.7
semgrep>=1.50
pip-audit>=2.7

# --- Optional: only needed to run the prompt-injection demo with --live ---
# anthropic>=0.40

# --- DELIBERATELY VULNERABLE dependency, for the pip-audit / SCA demo -----
# PyYAML 5.3.1 has known CVEs (e.g. CVE-2020-14343). It is NOT imported by
# any code here; it exists only so `pip-audit` has something real to flag.
# Remove it in any real project.
PyYAML==5.3.1

# Copy to .env (kept out of git) or export these before running app/app.py.
# Optional: override the dev session secret used by the CSRF section.
APP_SECRET_KEY=change-me-to-a-long-random-value
# Optional: run on a different port (default 5050; use 5000 only if AirPlay is off).
# PORT=5050

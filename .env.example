# Copy to .env (kept out of git) or export these before running app/app.py.
# Flip the whole app between the vulnerable and secured behaviours:
SECURE=0
# Used only in SECURE mode instead of a hardcoded key:
APP_SECRET_KEY=change-me-to-a-long-random-value
